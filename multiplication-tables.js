// For loop

for (let number = 1; number <= 10; number++) {

    for (let i = 1; i <= 10; i++) {
        const result = i * number;
        console.log(`${number} * ${i} = ${result}`);
    }
}